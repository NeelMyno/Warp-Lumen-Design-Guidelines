{
  "$schema": "https://design-tokens.github.io/community-group/format/tokens.schema.json",
  "rangeSlider": {
    "$description": "v0.7 — Single + dual-handle range slider. Two overlaid <input type='range'> for dual mode. The track is sunken; the active range fills lime; thumbs are white circles with a hairline border for grip affordance.",
    "track": {
      "$description": "The full-width 1 px-tall sunken bar behind the active range.",
      "height":     { "$value": "{dimension.1}", "$description": "4 px — readable but thin." },
      "radius":     { "$value": "{radius.full}" },
      "background": { "$value": "{color.surface.sunken}" }
    },
    "range": {
      "$description": "The lime fill between the two handles (or between min and the single handle).",
      "background": { "$value": "{color.accent.500}" }
    },
    "thumb": {
      "$description": "The drag handle. White circle with hairline border. Cursor changes to grab/grabbing on press.",
      "size":       { "$value": "{dimension.4}", "$description": "16 px — meets minimum touch target with hover-area extension." },
      "radius":     { "$value": "{radius.full}" },
      "background": { "$value": "#ffffff" },
      "border":     { "$value": "{color.border.strong}" },
      "shadow":     { "$value": "{shadow.sm}" },
      "ringFocus":  { "$value": "{shadow.focus}", "$description": "Lime focus ring around the thumb when keyboard-focused." }
    },
    "value": {
      "$description": "The numeric value labels rendered beneath the track (left + right).",
      "type":  { "$value": "{type.overline}" },
      "color": { "$value": "{color.text.tertiary}" }
    },
    "trackHeight": {
      "$description": "Vertical slot the track + thumbs share. Needs to be at least the thumb diameter so the thumb doesn't clip.",
      "$value": "{dimension.6}"
    }
  }
}
