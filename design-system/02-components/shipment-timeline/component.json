{
  "$schema": "../_schema/component.schema.json",
  "name": "ShipmentTimeline",
  "version": "0.13.0",
  "status": "stable",
  "deprecated": false,
  "summary": "Vertical timeline. Standard freight stages \u2014 pickup \u2192 cross-dock \u2192 line haul \u2192 last mile \u2192 delivered. Each stage: status (pending / active / done / failed), timestamp, optional ETA bubble. Active stage gets lime accent + pulsing LiveDot.",
  "props": {},
  "tokens": {
    "consumed": []
  },
  "a11y": {
    "wcag": [
      "1.4.3 Contrast (Minimum)",
      "2.1.1 Keyboard",
      "2.4.7 Focus Visible",
      "4.1.2 Name, Role, Value"
    ],
    "keyboard": [
      "Tab - cycle timeline nodes"
    ],
    "minTouchTarget": "44x44",
    "rules": [
      "Visible focus ring required (outline + box-shadow per AGENTS.md hard rule 11).",
      "Honor prefers-reduced-motion and prefers-reduced-transparency.",
      "Color is never the only signal \u2014 pair with label, shape, or icon."
    ]
  },
  "rules": {
    "do": [],
    "dont": [
      "NEVER use color alone for stage status \u2014 pair with icon or label.",
      "NEVER hide the rail between stages (the visual continuity is the point).",
      "NEVER fake an ETA \u2014 omit if unknown."
    ]
  },
  "examples": {
    "web-react": "./shipment-timeline.tsx"
  }
}
