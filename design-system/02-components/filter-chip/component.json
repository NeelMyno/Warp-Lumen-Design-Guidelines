{
  "$schema": "../_schema/component.schema.json",
  "name": "FilterChip",
  "version": "0.13.0",
  "status": "stable",
  "deprecated": false,
  "summary": "Applied-filter chip with operator + value display + optional dropdown editor + dismiss X. Shape is `{label} {operator-label} {value}`. Used in the row above a DataTable to show what's currently filtering the view.",
  "props": {},
  "tokens": {
    "consumed": []
  },
  "a11y": {
    "wcag": [
      "1.4.3 Contrast (Minimum)",
      "2.1.1 Keyboard",
      "2.4.7 Focus Visible",
      "4.1.2 Name, Role, Value"
    ],
    "keyboard": [
      "Tab - focus",
      "Space/Enter - toggle",
      "Backspace - remove (when removable)"
    ],
    "minTouchTarget": "44x44",
    "rules": [
      "Visible focus ring required (outline + box-shadow per AGENTS.md hard rule 11).",
      "Honor prefers-reduced-motion and prefers-reduced-transparency.",
      "Color is never the only signal \u2014 pair with label, shape, or icon."
    ]
  },
  "rules": {
    "do": [],
    "dont": [
      "NEVER omit onRemove \u2014 the point of a chip is dismissibility.",
      "NEVER use FilterChip for non-applied filters (use Tag).",
      "NEVER chain 10+ chips \u2014 use FilterBuilder."
    ]
  },
  "examples": {
    "web-react": "./filter-chip.tsx"
  }
}
