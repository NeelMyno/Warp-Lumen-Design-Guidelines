{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "snippet",
  "title": "Snippet",
  "description": "Lightweight terminal command or short code snippet. Built on shadcn's InputGroup; one-line copy affordance.",
  "dependencies": [
    "lucide-react"
  ],
  "registryDependencies": [
    "@lumen/utils",
    "@lumen/lumen-base"
  ],
  "files": [
    {
      "path": "design-system/02-components/snippet/snippet.tsx",
      "content": "/**\n * Lumen Snippet — Tier 5 AI primitive (Vercel AI Elements compatible)\n *\n * Lumen ships the contract (this file + ./snippet.md + ./snippet.skill.md).\n * Vercel AI Elements ships the React implementation. Install via:\n *\n *   npx ai-elements@latest add snippet\n *\n * Then import from `@/components/ai-elements/snippet` and theme via\n * <LumenAIProvider>. See:\n *   - ./snippet.md for the API contract\n *   - ./snippet.skill.md for NEVER rules\n *   - ../mode-scope/mode-scope.md for mode integration\n *   - ../../03-patterns/chat-thread.md for the canonical composition\n *\n * This file is a Lumen-side contract stub. It exists so that\n * `pnpm dlx shadcn@latest build registry.json` finds an existing file at\n * the registry item's `files[].path`. Consumers running\n * `npx shadcn add @lumen/snippet` get this file copied to\n * `components/ai-elements/snippet.tsx`. They then run the ai-elements\n * install above to overwrite the stub with the real implementation.\n *\n * Forking the Vercel implementation would lock Lumen to a snapshot; the\n * install-and-theme pattern keeps Lumen on Vercel's release train.\n */\n\nimport type { ReactNode } from \"react\";\n\nexport interface SnippetProps {\n  className?: string;\n  children?: ReactNode;\n}\n\n/**\n * @deprecated Lumen contract stub. Install the Vercel AI Elements implementation:\n *   `npx ai-elements@latest add snippet`\n * Then import from `@/components/ai-elements/snippet`.\n */\nexport function Snippet(_props: SnippetProps): never {\n  throw new Error(\n    \"[Lumen] <Snippet> is a contract-only stub. \" +\n    \"Install the Vercel AI Elements implementation: `npx ai-elements@latest add snippet`. \" +\n    \"Then import { Snippet } from `@/components/ai-elements/snippet`.\"\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/ai-elements/snippet.tsx"
    }
  ],
  "cssVars": {
    "light": {},
    "dark": {}
  },
  "meta": {
    "lumenVersion": "0.13.0",
    "tier": "T5",
    "family": "Content",
    "phase": 5,
    "vercelAIElements": {
      "name": "Snippet",
      "installCommand": "npx ai-elements@latest add snippet"
    },
    "platforms": [
      "web-react"
    ]
  },
  "type": "registry:ui"
}