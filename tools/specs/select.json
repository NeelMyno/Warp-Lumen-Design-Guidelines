{
  "name": "select",
  "title": "Select",
  "namedExport": "Select",
  "category": "form",
  "tier": "T2",
  "status": "stable",
  "summary": "Single-select dropdown built on Radix Select. v0.6 field-shell trigger. Portaled content. For searchable lists use @lumen/combobox.",
  "description": "Token-driven single-select dropdown built on Radix Select.",
  "skillDescription": "Use for single-select with ≤ 8 options. For longer lists or where search helps, use @lumen/combobox. The trigger adopts the v0.6 field-shell visual; the portaled content uses the same lime focus tinting as other menus.",
  "builtOn": "Radix Select",
  "modeAware": false,
  "registryType": "registry:ui",
  "dependencies": ["@radix-ui/react-select", "lucide-react"],
  "registryDependencies": ["@lumen/utils"],
  "platforms": ["web-react"],
  "tokensConsumed": [
    "surface.input.rest",
    "surface.input.disabled",
    "surface.popover",
    "surface.tint-accent",
    "border.default",
    "border.strong",
    "border.focus",
    "border.error",
    "text.primary",
    "text.tertiary",
    "text.placeholder",
    "shadow.input.lit-edge",
    "shadow.input.focus",
    "shadow.popover",
    "radius.md",
    "radius.xs"
  ],
  "useWhen": ["Single-select with ≤ 8 options.", "Form fields where typing isn't the affordance.", "Inline option pickers (sort by, page size)."],
  "anatomy": ["Select root", "SelectTrigger (field-shell visual)", "SelectValue (renders the selected label / placeholder)", "SelectContent (portaled)", "SelectItem × N", "SelectGroup + SelectLabel + SelectSeparator"],
  "api": [
    "`value`, `onValueChange` — controlled.",
    "`defaultValue` — uncontrolled.",
    "`size` — sm | default on SelectTrigger.",
    "Standard Radix Select props."
  ],
  "never": ["NEVER use Select for >8 options — use Combobox.", "NEVER render content inline (Radix portals).", "NEVER omit the chevron icon (it's the affordance)."],
  "a11yRules": ["Radix manages role=combobox + role=listbox + aria-* + arrow-key + type-ahead.", "SelectValue shows the selected item or placeholder for screen readers."],
  "do": ["Use for ≤ 8 options.", "Pair with <Label> via <Field>."],
  "dont": ["Don't use for >8 options.", "Don't render content inline."],
  "related": ["Combobox", "DropdownMenu", "RadioGroup"],
  "stories": [{ "name": "Default", "args": {}, "children": "" }],
  "variants": ["default", "sm", "with-groups"],
  "propsList": [
    { "name": "value", "type": "string" },
    { "name": "onValueChange", "type": "(v: string) => void" },
    { "name": "defaultValue", "type": "string" }
  ],
  "exampleSnippet": "import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from \"@/components/ui/select\";\n\nexport function Example() {\n  return (\n    <Select>\n      <SelectTrigger>\n        <SelectValue placeholder=\"Service\" />\n      </SelectTrigger>\n      <SelectContent>\n        <SelectItem value=\"ltl\">LTL</SelectItem>\n        <SelectItem value=\"ftl\">FTL</SelectItem>\n      </SelectContent>\n    </Select>\n  );\n}",
  "testChildren": ""
}
