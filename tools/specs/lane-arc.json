{
  "name": "lane-arc",
  "title": "LaneArc",
  "namedExport": "LaneArc",
  "category": "freight-domain",
  "tier": "T4",
  "status": "stable",
  "summary": "SVG arc renderer over a faint base map. The arc draws in on mount via stroke-dasharray animation (cubic-bezier(0.2,0,0,1), 1200ms). Lime-accent tinted by default. Origin + destination markers at endpoints. Honors prefers-reduced-motion (arc renders instantly).",
  "description": "SVG lane arc with mount-time draw-in animation.",
  "skillDescription": "Use to visualize a freight lane as a curved arc on a base map (or empty canvas). The animated draw-in is a peak-end moment — landing heroes, route-overview surfaces. For raw code-only label use @lumen/lane-code.",
  "builtOn": "Native SVG + CSS animation",
  "modeAware": false,
  "registryType": "registry:ui",
  "dependencies": [],
  "registryDependencies": ["@lumen/utils"],
  "platforms": ["web-react"],
  "tokensConsumed": ["color.accent", "text.tertiary", "surface.sunken"],
  "useWhen": ["Landing hero — animated route arc on a map placeholder.", "Route overview surface — single arc highlight.", "Empty state on a routes surface — illustrative arc."],
  "anatomy": ["SVG canvas (configurable width × height)", "Quadratic Bezier curve (computed from start, end, optional control)", "Origin marker (circle)", "Destination marker (filled circle + arrow)", "Stroke-dasharray reveal animation"],
  "api": [
    "`start` — { x, y } 0–1 normalized coords or px.",
    "`end` — { x, y }.",
    "`curve` — 0–1 curve height (default 0.3).",
    "`width`, `height` — canvas px (default 320 × 160).",
    "`color` — stroke color (default --color-accent).",
    "`animated` — boolean (default true).",
    "`label` — optional alt text."
  ],
  "never": ["NEVER use LaneArc as a static decoration — the animation is the point.", "NEVER stack more than 3 arcs (visual noise).", "NEVER animate faster than 800ms."],
  "a11yRules": ["role=img + aria-label='Route from {origin} to {destination}'.", "Honors prefers-reduced-motion (no draw-in)."],
  "do": ["Use on hero surfaces.", "Provide aria-label."],
  "dont": ["Don't stack 4+.", "Don't speed faster than 800ms."],
  "related": ["LaneCode", "RouteMap", "ShipmentTimeline"],
  "stories": [{ "name": "Default", "args": {}, "children": "" }],
  "variants": ["default", "curved", "straight"],
  "propsList": [
    { "name": "start", "type": "{x, y}" },
    { "name": "end", "type": "{x, y}" },
    { "name": "curve", "type": "number", "default": 0.3 },
    { "name": "width", "type": "number", "default": 320 },
    { "name": "height", "type": "number", "default": 160 },
    { "name": "color", "type": "string" },
    { "name": "animated", "type": "boolean", "default": true },
    { "name": "label", "type": "string" }
  ],
  "exampleSnippet": "import { LaneArc } from \"@/components/ui/lane-arc\";\n\nexport function Example() {\n  return <LaneArc start={{ x: 0.1, y: 0.8 }} end={{ x: 0.9, y: 0.2 }} label=\"LAX to SFO\" />;\n}",
  "testChildren": ""
}
