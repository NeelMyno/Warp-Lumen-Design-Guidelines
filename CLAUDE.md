# CLAUDE.md — Lumen-specific addenda for Claude

> Read [AGENTS.md](./AGENTS.md) first. Everything in AGENTS.md applies. This file adds Claude-specific instructions on top.

> [!warning]
> ## Docs ↔ Dashboard sync is non-negotiable (v0.14 R11)
>
> **Whenever a foundation document changes, the audit-dashboard must be updated in the same change. Whenever the audit-dashboard changes a primitive's visual or contract, the corresponding foundation document must be updated in the same change. Token JSON files, CSS variables in `globals.css`, foundation prose, and the live foundations render must all stay in sync at all times.**
>
> This is the meta-rule the user codified in R11. Pre-R11 the system had drift: `elevation.md` §7 documented the focus ring as a "lime alpha-32" recipe while the runtime had been (correctly) retuned to outline + box-shadow per ADR 0021 — the doc lagged the code by ~8 weeks. R11 closed that drift AND raised the bar: future changes must move together.
>
> **Concretely — what "sync" means**:
>
> | When you change… | You MUST also update… |
> |---|---|
> | `design-system/00-foundations/{file}.md` prose | The matching `audit-dashboard/src/app/foundations/page.tsx` showcase section, plus the live CSS rule in `audit-dashboard/src/app/globals.css`, plus the underlying token JSON in `design-system/01-tokens/`. |
> | `audit-dashboard/src/components/primitives/{name}.tsx` visual or API | The `design-system/02-components/{name}/component.md` prose + `component.json` schema, plus any cross-reference in `design-system/00-foundations/*.md`, plus the showcase in `audit-dashboard/src/app/library/client.tsx` and (if relevant) `audit-dashboard/src/app/foundations/page.tsx`. |
> | `design-system/01-tokens/**/*.tokens.json` | The CSS variables in `audit-dashboard/src/app/globals.css` (the runtime mirror), plus the foundation doc that prose-describes the token, plus the foundations showcase that demos it, plus `TOKEN-INDEX.md` (auto-regenerated via `pnpm token-index`). |
> | `audit-dashboard/src/app/globals.css` shadow / border / surface tokens | The DTCG source-of-truth in `design-system/01-tokens/`, AND the matching prose in `design-system/00-foundations/elevation.md` / `color.md` / `accessibility.md`, AND the foundations showcase that demos the token. |
> | `AGENTS.md` hard rule | The audit-dashboard's `AGENTS.md` (`20 hard rules` link), the audit-dashboard's `README.md`, any foundation doc that cross-references the rule, and an ADR documenting the rationale. |
>
> **Why this matters**: every drift between docs and runtime makes the system harder for future agents to reason about. An LLM reading `elevation.md` sees one rule; reading `globals.css` sees a different rule; the system internally contradicts itself; trust in the documentation collapses. The R11 mandate is that we never ship a change to one layer without updating the other layers in lockstep. This is enforced socially (in code review + via this CLAUDE.md note) and structurally (via the `lint:shadow-no-accent` + `validate:tokens` scripts that cross-check token JSON against the prose claims).
>
> **The R11 ship itself is the first proof-point**: 15+ files (CSS, DTCG tokens, foundation docs, ADR 0030, AGENTS.md hard rules, the lint script, the foundations Elevation showcase, the LumenMark hover, the slider primitive) all moved in one commit to retire green from every shadow. Future mandates land the same way.

> [!note]
> ## v0.14.2 — R13: LazyMount paint-flash fix + Elevation perceptual lift + synthetic-names cleanup (current state)
>
> [ADR 0032](_meta/decisions/0032-r13-lazy-mount-paint-flash-elevation-lift-v0142.md) ships the second multi-route Claude-in-Chrome MCP audit's closures — three real-world bugs that R12 either introduced or left in place. R13 validates the R12 methodology rule: a fix that ships in round N may surface a new bug class in round N+1; the audit-via-MCP loop turns that pattern from regression to refinement.
>
> **R13-001 — LazyMount paint-defer flash on fast scroll (P0).** R12 migrated LazyMount from React-state IntersectionObserver to pure CSS `content-visibility: auto`. The R12 fix correctly closed the SSR contract gap AND preserved the R8c first-paint LCP win. But `content-visibility: auto` is a **paint-perpetual** contract — it skips paint for off-screen elements **every frame, indefinitely**, not just at first paint. During fast scroll (10+ wheel-ticks/sec via Magic Mouse / touchpad), the browser's paint-prediction lags behind viewport movement. The user lands in a scroll position where multiple lazy-mounted sections fill the viewport — the browser hasn't finished painting them yet, so the entire viewport reads as a **black void** for one or more frames. Reproduced **7+ times** across `/library` in a single top-to-bottom audit walk. R13 swaps the implementation to **first-paint-only deferral**: SSR + initial render apply `content-visibility: auto` (preserves R8c LCP win + R12 SSR-completeness contract); after hydration, a `useEffect` schedules a `requestAnimationFrame` callback that flips the style to `{}`, removing content-visibility from every LazyMount. Subsequent scrolls have **no paint-defer, no black voids, no viewport latency**. JS-disabled users still get the SSR-shipped paint-deferred contract (functional with potential void). Default `placeholderHeight` reduced from 600 → 240; all 23 `/library` wraps updated from `{500}` → `{240}` for tighter first-paint reservation. Live verification: `document.querySelectorAll('[data-lazy-mount]')` returns 23 elements, all with `data-lazy-mount="ready"` post-hydration (was `"lazy"` pre-fix, indicating content-visibility was still applied perpetually).
>
> **R13-002 — Foundations Elevation showcase visually undifferentiated on dark canvas (P1, closes R12-002 deferred).** After R11 retired green from every shadow token, the 6 elevation cards (xs / sm / md / lg / xl / 2xl) became neutral black. On the obsidian canvas (#0d0d0d), `rgba(0,0,0,0.32)` shadows blend almost perfectly into the background — the token-space ladder is correct, but the pixel-space ladder is invisible. R12 deferred this as P2; R13 closes it. Each card now layers an inset top highlight that scales with the shadow ladder, alongside the unchanged ladder shadow: `boxShadow: inset 0 1px 0 0 rgba(255, 255, 255, ${insetAlpha}), var(--shadow-${level})`. insetAlpha per level: xs=0.04, sm=0.09, md=0.14, lg=0.20, xl=0.26, 2xl=0.32. **Demo-only treatment** — underlying shadow token values are unchanged, and consumer apps don't inherit this inset highlight. It's a per-card visual cue that scales monotonically with the elevation ladder, communicating lift at the cream-channel where neutral shadows alone fall silent on the dark canvas. In light mode, dark shadows do most of the work; the inset white highlight at 4-32% alpha is barely noticeable — graceful no-op.
>
> **R13-003 — Real-person names re-leaked into fixtures (P1).** The v0.12.5 contract retired real-person names from all fixtures, examples, and demos in favor of synthetic operator names (Avery Mercer / Kai Morgan style). R13 caught two remaining sites where "Daniel" had re-leaked: `audit-dashboard/src/components/primitives/display.tsx:485` Timeline event `actor: "Daniel S."` (now "Avery M.") and `audit-dashboard/src/components/primitives/ai.tsx:214` TypingIndicator default prop `name = "Daniel"` (now "Avery"). Continues v0.12.5 cleanup. Avatar palette is name-hashed so deterministic colors follow whatever name ships.
>
> **R13 methodology contribution to the audit-cycle ladder:** R12's rule was *a doc that describes the intent and a runtime CSS that implements the intent can drift silently; the live multi-route MCP audit is the enforcement mechanism*. R13 extends: ***a fix that ships in round N may surface a new bug class in round N+1 — that's the audit-via-MCP ladder working as designed. R13 closes the R12 paint-defer flash without giving up either the R12 SSR contract or the R8c LCP win, by combining the SSR-complete DOM + first-paint-only deferral into a hybrid that's strictly better than either prior implementation.***
>
> **R13 deferred:** R13-004 — foundation showcases for OS-modes / Print / i18n / Responsive / State-matrix (P2). Prose docs exist (`os-modes.md`, `print.md`, `internationalization.md`, `responsive.md`, `state-matrix.md`); live showcases on `/foundations` do not. Significant new authoring work — future round closes.
>
> **Validation:** `pnpm validate:tokens` → 956 tokens valid; `pnpm lint` → all 8 lint rules pass; `pnpm exec tsc --noEmit` (audit-dashboard) → PASS; `pnpm build` → 12 routes prerender; `pnpm exec playwright test` → 56 / 58 pass (2 skipped on axe-core gate). 30 ADRs total.

> [!note]
> **v0.14.1 — R12 dark text-ladder + LazyMount SSR-completeness (predecessor).** [ADR 0031](_meta/decisions/0031-r12-dark-text-ladder-and-lazy-mount-ssr-v0141.md) shipped the first multi-route Claude-in-Chrome MCP audit's closures.
>
> **R12-001 — dark text-tertiary collapsed to text-secondary.** Audit caught `audit-dashboard/src/app/globals.css:630` mapping both `--text-secondary` AND `--text-tertiary` to `var(--lumen-obsidian-3)` (#9A9A9A) in dark mode — the 3-tier text ladder declared by the DTCG semantic spec (`text.secondary` = brand.300, `text.tertiary` = brand.400) collapsed to 2 tiers at runtime. R12 restores the ladder: `--text-tertiary` → `var(--lumen-obsidian-4)` (#6B6B6B, 3.65:1 AA Large), `--text-placeholder` → `obsidian-4`, `--text-disabled` → kept at `obsidian-4`; DTCG `text.disabled` realigned from `brand.500` (#404040, 1.89:1) to `brand.400` to match runtime. Primitive comments at globals.css:98–99 corrected (obsidian-3 = secondary role, obsidian-4 = tertiary+disabled role; the comments had them swapped pre-R12). Light mode was already correct. Live verification: foundations Color showcase now renders distinct hex values for `text.secondary` (#9A9A9A) vs `text.tertiary` (#6B6B6B). Every UI that uses tertiary text (eyebrow labels, captions, hints, "ON THIS PAGE" rail headers, KPI sub-labels) reads at the documented 3.65:1 contrast tier, quieter than secondary text — the hierarchy.md "1.5–2× weight rule" works visually again.
>
> **R12-003 — LazyMount renders empty placeholders on SSR.** The R8c LazyMount component (ADR 0029) had a doc claim of SSR-safety via `typeof IntersectionObserver === "undefined"` fallback inside `useEffect` — but `useEffect` doesn't run on the server, so SSR shipped 23 empty placeholder divs on /library and the lazy sections were missing from the initial HTML (`curl /library | grep "Get rates\|Save draft\|Tab bar\|Auto-quote\|Liftgate at\|Contract rate\|Email me when\|Pick or type"` returned 0 hits pre-R12). Consequences: Cmd+F broken outside the above-the-fold tranche; screen-reader pre-walk only announced 2 sections + 23 nameless `aria-hidden` placeholders; JS-disabled users saw permanently-empty placeholders; fast-scroll users outran the rootMargin=400px buffer and landed in unmounted placeholders. R12 rewrites LazyMount to use **CSS `content-visibility: auto`** + `contain-intrinsic-size` instead of React-state-driven conditional rendering. Children are ALWAYS in the DOM (SSR ships full content, Cmd+F works, screen readers walk, JS-disabled works); the browser's layout engine handles paint-deferral at the CSS level — equivalent lazy-paint behavior to IntersectionObserver, at the engine layer instead of the React-state layer. The `eager` prop is preserved (no content-visibility on above-the-fold). The `rootMargin` prop is removed (the browser's native near-viewport heuristic handles it). **SSR HTML on /library: 91,854 → 564,618 bytes (+6.1×).** All 7 sampled lazy-section strings now appear in SSR. Browser support: Chrome/Edge 85+, Safari 18+, Firefox 125+. Older browsers fall back to full eager rendering (paying the layout cost but still seeing content — graceful).
>
> **R12 methodology contribution to the audit-cycle ladder:** R12 is the first round driven by a live multi-route audit through the Claude in Chrome MCP — earlier rounds were user-reported, spot-issue-driven, or Lighthouse-driven. The methodology rule: ***a doc that "describes the intent" and a runtime CSS that "implements the intent" can drift silently — the only way to know is to walk every route with eyes on the rendered pixels AND probe the live token values via `getComputedStyle(documentElement).getPropertyValue(...)`. The R11 docs↔code sync mandate codifies the rule; R12 adds the verification surface (live audit through MCP) as the enforcement mechanism.***
>
> **R12 deferred:** R12-002 — foundations Elevation showcase row reads visually undifferentiated across xs/sm/md/lg/xl/2xl tokens on the obsidian canvas (P2). The tokens are mathematically correct per R11; the issue is perceptual on near-black canvas. A future round can add per-card visual hints (inset top highlight) to communicate lift better. Token values unchanged.
>
> **Validation:** `pnpm validate:tokens` → 956 tokens valid; `pnpm lint` → all 6 lint rules pass; `pnpm exec tsc --noEmit` (audit-dashboard) → PASS; `pnpm build` → 12 routes prerender; `pnpm exec playwright test` → 56 / 58 pass (2 skipped on axe-core gate).

> [!note]
> **v0.14 R11 was the predecessor — current contract for shadows.** Green retired from every shadow color value. The focus ring is now theme-aware neutral (`--border-frame` outline + neutral box-shadow halo). The primary-CTA accent glow ladder is now neutral elevation (`shadow.accent-glow` aliases `{shadow.lg}`). AI shimmer pulses border-frame ↔ border-strong. Brand-mark hover uses paper-alpha + brightness lift. Slider thumb hover/focus rings are neutral. The `lint:elevation-no-accent` script from R10 was renamed `lint:shadow-no-accent` and broadened to scan EVERY shadow token. See [ADR 0030](_meta/decisions/0030-no-green-shadows-and-docs-code-sync-v014-r11.md).

> [!note]
> **v0.14.0 — Omnibus systemic-gap closure (current).** [ADR 0029](_meta/decisions/0029-v014-omnibus-systemic-gap-closure.md) codifies the first MINOR-version bump since v0.12.0 (Obsidian recolor, Nov 2025). v0.14 closes 9 independent systemic gaps in one ship, after chat 42's "what's missing from chat 39" gap-audit surfaced them. **The 9 axes shipped:** (1) **R8c — `/library` DOM weight reduction** via new SSR-safe [`<LazyMount>`](audit-dashboard/src/components/lazy-mount.tsx) at `audit-dashboard/src/components/lazy-mount.tsx`. IntersectionObserver-driven mount on scroll-into-view; wraps 23 below-the-fold `<Section>` blocks in /library (first 2 stay eager). `/library.html`: 1044 KB → 575 KB (−45%); LCP 3634 → 1815 ms (−1819 ms / −50% — R8b regression fully closed). (2) **R8d — italic font-display: optional** at [`audit-dashboard/src/app/layout.tsx`](audit-dashboard/src/app/layout.tsx). Split next/font into regular satoshi (preload: true, display: swap) + italic satoshi-italic (preload: false, display: optional). Italic dropped from LCP critical path. globals.css `--font-sans` chain extended with `var(--font-satoshi-italic)` for italic fallback. (3) **R9 — Reduced-motion + high-contrast + forced-colors OS-mode contracts** via new [`design-system/00-foundations/os-modes.md`](design-system/00-foundations/os-modes.md) + two new `@media` blocks in globals.css. `prefers-contrast: more`: border-hairline → default, focus outline thickens 2→3 px + offset 1→2 px, tertiary text upgrades to secondary contrast, decorative box-shadow halos retire. `forced-colors: active`: structural `1px solid CanvasText` borders on every interactive surface, `box-shadow: none !important`, OS-controlled `Highlight`/`HighlightText` for primary CTAs, `GrayText` for disabled. Closes the 0-occurrence gap both modes had across the system. (4) **R10 — Print stylesheet + export contracts** via [`design-system/00-foundations/print.md`](design-system/00-foundations/print.md) + `@media print` block in globals.css + `[data-export="image"]` attribute pattern. Forces light theme regardless of `data-theme="dark"`, retires dashboard chrome, collapses grids to single column, retires shadows in favor of crisp 1 px borders, prints URLs after external links, honors page-break boundaries at section level. (5) **R11 i18n + RTL scaffold** via new [`design-system/00-foundations/internationalization.md`](design-system/00-foundations/internationalization.md) + CSS scaffold: `[dir="rtl"] [data-rtl-flip]` mirrors directional icons; `[data-numeric]` pins LTR direction inside RTL paragraphs; `[data-lumen-sidebar]` uses inset-inline-* logical positioning; `.lumen-bidi-isolate` for mixed-direction strings. Disclaimer: Lumen has NOT been visually audited at `dir="rtl"` — carried to i18n.2. (6) **Templates layer expansion** — 3 new pattern docs ([`error-pages.md`](design-system/05-patterns/error-pages.md), [`email-layout.md`](design-system/05-patterns/email-layout.md), [`empty-state-flow.md`](design-system/05-patterns/empty-state-flow.md)) + new auto-generated [`PATTERN-INDEX.md`](PATTERN-INDEX.md) catalog + `pnpm pattern-index` generator + Lumen-branded 404 + 500 implementations at `audit-dashboard/src/app/{not-found,global-error}.tsx`. 10 patterns total. (7) **Cross-platform examples — 9 new files** — Button + Card + Field × ios-native (SwiftUI) + android-native (Compose) + react-native. Each mirrors the web-react contract. component.json `examples` field declares 4 platforms each. Closes 98/98 web-react vs 0/98 native gap as proof-of-concept tranche. (8) **MCP server** at [`mcp/server.mjs`](mcp/server.mjs) — stdio JSON-RPC 2.0, pure-Node, zero new deps. 9 tools (list/get for components/tokens/ADRs/patterns + free-text search) + resource URIs (`lumen://components/{slug}`, etc.). Closes USING-LUMEN.md hard rule 4's "the MCP server reads" reference (was aspirational, now structural). Smoke-tested. Wires to Claude Desktop / Claude Code / Cursor via standard MCP config. (9) **Testing infrastructure + npm distribution scaffold**: [`audit-dashboard/playwright.config.ts`](audit-dashboard/playwright.config.ts) + 10-test smoke suite + 8-test a11y baseline (18/19 pass; 1 skipped gated on `@axe-core/playwright`); Storybook scaffold + first Button story; [`packages/lumen-tokens/`](packages/lumen-tokens/) npm package re-exporting `_build/{ts,json,css}/`. **Cumulative Lighthouse R7 → v0.14: LCP geo-mean 3030 → 2068 ms = −962 ms / −31.7%** (a third of LCP eliminated in one week across R7 baseline + R8a + R8b + v0.14 omnibus). `/foundations` 3300 → 1896 ms (−1404 ms). `/library` 3328 → 1815 ms (−1513 ms — slowest route to one of the fastest). `/saas` 3257 → 1992 ms (−1265 ms). **CLS 0.000 on every route, every config, every test run** — metric-aligned fallback contract (ADR 0010) holds end-to-end. **AGENTS.md hard rules: 16 → 19** (new: 17 LazyMount on DOM-heavy routes, 18 OS modes + print at system layer, 19 italic family fallthrough + i18n scaffold). **v0.14 omnibus methodology rule:** *when the gap-audit surfaces ≥ 5 independent items in one cycle, ship them as an omnibus minor with a single multi-axis ADR; future cycles return to per-axis ADRs.* The signal that omnibus is the right shape: items share a "structural completeness" theme rather than perf or correctness. **v0.13.5 was the predecessor — R8b critical-CSS inlining for LCP round-trip elimination.** [ADR 0028](_meta/decisions/0028-inline-css-mobile-perf-v0135.md) codifies the ninth round of the audit-cycle ladder, the second follow-up on the R7 mobile-perf baseline (R8a was the first). R8b's tool axis: **Next.js 16's `experimental.inlineCss: true`** (one config flag in [`audit-dashboard/next.config.ts`](audit-dashboard/next.config.ts)) + re-Lighthouse via the new re-runnable [`scripts/lighthouse-mobile-baseline.mjs`](scripts/lighthouse-mobile-baseline.mjs) (which factors the chat 41 inline bash recipe into a single CLI script supporting `--tag`, `--port`, `--routes`). The R8a baseline left "critical-CSS inlining" as the next-largest LCP lever: the 26 KB Tailwind utility chunk wasted 462–635 ms per route per the Lighthouse `render-blocking-resources` audit. **The R8b closure:** `experimental.inlineCss: true` replaces every prerendered page's `<link rel="stylesheet">` with a `<style data-precedence="next">` block inline in `<head>`. The atomic-CSS regime (Tailwind v4) and the single-visit access pattern (the audit-dashboard is a review surface, not a returning-visitor app) are exactly the cases the Next.js docs describe as the inlineCss sweet spot — and the result confirms that characterization. **Across 9 routes at the Moto G4 4G profile, 3-run medians: LCP geo-mean 3001 → 2706 ms (−295 ms, −9.8%).** Per-route standouts: `/landing` 2954 → 1825 ms (−1129 ms), `/commerce` 2948 → 1898 ms (−1050 ms), `/desktop` −241 ms, `/mobile` −234 ms, `/foundations` 3-run geomean −143 ms, `/saas` and `/tool` near-flat (noise floor), `/` −83 ms (the `/foundations` redirect still costs ~200 ms regardless). **Speed Index improvements are the deeper UX story:** `/` SI 2614 → 1483 ms (−1131 ms), `/library` SI 2575 → 1648 ms (−927 ms) — the browser paints progressively faster even where the LCP candidate paints last. **CLS contract held: 0.000 on every route, both configs.** Both `@font-face` blocks (the handcoded `Satoshi-Fallback` from globals.css per ADR 0010, AND next/font's auto-generated `satoshi Fallback`) are preserved verbatim in the inlined CSS — the `size-adjust` + `ascent-override` + `descent-override` metric overrides are font-table-derived (hhea + OS/2 → next/font reads at build time), not glyph-derived, so inlining doesn't perturb the CLS-critical values. Verified `grep -c "size-adjust"` on the produced HTML: 4 hits per route, all inline. **The `/library` outlier:** R8b's only regression is `/library` (LCP +184 ms median / +446 ms 3-run-geomean). LCP element selector is identical to R8a (`div.grid > article > header > p.mt-4`); root cause is main-thread CSS parse cost — under external CSS the browser parses 26 KB of utility rules on the network thread in parallel with HTML download, under inline CSS the same parse cost lands on the main thread before applying styles to /library's 4684-node DOM. Documented in [ADR 0028](_meta/decisions/0028-inline-css-mobile-perf-v0135.md) with the diagnostic numbers — main-thread work breakdown 3138 → 6283 ms (2×), bootup time 332 → 1029 ms (3×). **This regression is bounded:** the R8c carry-forward already names `/library` DOM weight reduction (Intersection Observer lazy-rendering of the 98 primitive showcases) as the next round. Once `/library`'s above-the-fold DOM is on the order of 200 nodes instead of 4684, the inlineCss main-thread cost stops dominating and `/library` rejoins the win column. Audit baseline at [`.audit-runs/2026-05-19-round-8b/LIGHTHOUSE.md`](.audit-runs/2026-05-19-round-8b/LIGHTHOUSE.md). **Cumulative across R8a + R8b: LCP geo-mean 3030 → 2706 ms (−324 ms, −10.7%) from the R7 baseline.** **Process improvement:** [`scripts/release.mjs`](scripts/release.mjs) now supports the `--banner-only` flag (closes the chat 41 friction). When set, the script uses the CURRENT VERSION value as authoritative (no bump), skips the CHANGELOG mutation entirely, and skips the `[next]`-already-exists guard. Use the new flag for the "pre-author the CHANGELOG block, then sync banners" workflow common to performance-round releases where the audit log informs the prose before the version-bump moment; the default `release.mjs patch` flow is unchanged. **R8b methodology contribution to the audit-cycle ladder:** R8a's rule was *consumer-side metric soft-spots have measurable byte-level levers. Subsetting, inlining, splitting, lazy-loading — each is a discrete byte-saving move. A round can pull multiple levers; ship the diff that's largest per unit-of-architectural-risk.* R8b extends: ***byte-level levers also have shape — moving bytes between the network thread and the main thread changes WHERE the cost lands, not just WHETHER it lands. A round can be a clean win in aggregate while regressing on the heaviest route in the surface set; that regression is a signal for the NEXT lever, not a veto on the current one.*** **R8c+ candidates** rank by expected LCP impact ÷ architectural-risk: (1) **R8c — `/library` DOM weight reduction** (Intersection Observer lazy-render of the 98 primitive showcases — 300–500 ms `/library` LCP win, also compensates the R8b main-thread regression); (2) **R8d — italic `font-display: optional`** (brand call, saves italic from the critical path on every route); (3) **R8e — real iOS Safari + real Android Chrome verification** (BrowserStack or SauceLabs); (4) **R9 — reduced-motion + high-contrast OS-mode contracts**; (5) **R10 — print + export contracts**; (6) **R11 — native pipeline render verification (Swift / Compose / Flutter)**. **v0.13.4 was the predecessor — R8a Satoshi subset for LCP critical-path bytes.** [ADR 0027](_meta/decisions/0027-satoshi-subset-mobile-perf-v0134.md) codifies the eighth round of the audit-cycle ladder. R8a's tool axis: **`scripts/subset-satoshi.mjs`** (declarative codepoint keep set + `python3 -m fontTools.subset --layout-features=* --flavor=woff2`) + re-Lighthouse via `node_modules/.bin/lighthouse` CLI with the Moto G4 4G profile. Where R7 (v0.13.3) **captured** the first mobile Lighthouse baseline and identified LCP (3030 ms geo-mean) as the only Needs-Improvement metric, **R8a moves it**. The R7 hypothesis (Satoshi font payload on the LCP critical path) was diagnosed via `largest-contentful-paint-element` + `network-requests` + `lcp-breakdown-insight` audits and surgically addressed. **The R8a closures:** (1) **Satoshi VF re-subset.** Both `audit-dashboard/src/fonts/Satoshi-Variable.woff2` and `Satoshi-VariableItalic.woff2` re-subset to drop unused codepoints + glyphs while preserving every Lumen-referenced OpenType feature. Pre/post sizes: regular 42,588 → 29,964 bytes (−30%), italic 43,844 → 30,840 bytes (−30%). **Total bytes saved on the critical path: 25,628 bytes** — roughly 125 ms transfer at 1.6 Mbps simulated mobile. Codepoints in subset: 230 (down from 431). Glyphs: 303 (down from 504). The declarative keep set, codified in [`scripts/subset-satoshi.mjs`](scripts/subset-satoshi.mjs): ASCII + Latin-1 supplement (every common Western European accented letter — é à ñ ü ö ç í ø å) + General Punctuation (em dash, en dash, smart quotes, ellipsis, bullet) + Currency Symbols (€ £ ¥ ₹ $ ¢ ₩ ฿…) + Letterlike Symbols (© ® ™ ℗ ℠) + Basic arrows (↑ ↓ ← → ↔) + used Math operators (− ∞ ≈ ≠ ≤ ≥) + the geometric shapes Satoshi shipped (■ □ ▲ △ ◊ ○ ●) + ✓ +  Apple PUA. Coverage gaps (intentional drops, falling through to system Arial): Latin Extended-A (Polish/Czech/Hungarian/Romanian/Turkish text), Latin Extended-B, IPA, combining diacritics (Latin-1 covers precomposed forms), Greek (Ω/π — fine in fallback), box-drawing chars Satoshi never shipped anyway. **Every Lumen-referenced OpenType feature preserved**: kern, liga, calt, ss01–ss04 (Satoshi alt-G/F/etc.), tnum, lnum, sinf, sups, frac, case, locl, salt, dnom, numr. **Variable axis preserved**: wght 300–900. (2) **Re-Lighthouse R8a baseline** across all 9 audit-dashboard routes at the same Moto G4 4G mobile profile. **LCP geo-mean: 3030 → 2861 ms (−169 ms, −5.6%).** Per-route standout: `/foundations` 3300 → 2728 ms (**−572 ms** — drops out of "Needs Improvement" entirely, sits 228 ms from the 2500 ms Good threshold). 7 of 9 routes improved 100–200 ms; 1 nearly flat (`/tool`), 1 flat (`/commerce`). Perf score: 94 → 95 geo-mean. **CLS contract held: 0.000 on every route** — the metric-aligned `Satoshi-Fallback` (ADR 0010) is font-table-derived (hhea + OS/2), not glyph-derived; subsetting drops glyph data but not the font-level metrics next/font auto-computes from. Audit baseline at [`.audit-runs/2026-05-18-round-8/LIGHTHOUSE.md`](.audit-runs/2026-05-18-round-8/LIGHTHOUSE.md). The pre-subset originals are preserved at [`.audit-runs/_font-backups/`](.audit-runs/_font-backups/) for recovery. **What was deliberately NOT changed in v0.13.4:** (a) the handcoded `Satoshi-Fallback` `@font-face` block in `globals.css` — shadowed by next/font's auto-generated `satoshi Fallback` in the audit-dashboard but referenced by `design-system/01-tokens/primitives/typography.tokens.json` as the active metric-aligned fallback for Lumen consumers self-hosting Satoshi without next/font; removing it would break the ADR 0010 contract; (b) the next/font config in `audit-dashboard/src/app/layout.tsx` — already optimal (`display: "swap"`, `preload: true` for both variants); splitting into two `localFont()` calls to drop italic preload was considered but rejected — italic is small post-subset (30 KB), splitting introduces a separate font-family + extra `@font-face` block, the marginal LCP win wouldn't justify the architectural complexity (carried to R8b); (c) typography contracts in `design-system/00-foundations/typography.md` + `_meta/glossary.json` — every Lumen-referenced OT feature is preserved in the subset (no Foundations or `lumen-mono` ladder rules need updating). **R8a methodology contribution to the audit-cycle ladder**: R7's rule was *passing validators is a producer-side green; passing consumer-side metrics is a separate axis. Both ship together.* R8a extends: ***consumer-side metric soft-spots have measurable byte-level levers. Subsetting, inlining, splitting, lazy-loading — each is a discrete byte-saving move. A round can pull multiple levers; ship the diff that's largest per unit-of-architectural-risk.*** The R7→R8a transition is the first concrete *measure-and-improve* pair in the ladder — prior rounds shipped fixes; R7 shipped the first metric baseline; R8a shipped the first metric improvement against that baseline. **R8b+ candidates** rank by expected LCP impact ÷ architectural-risk: (1) **critical-CSS inlining** — the 26 KB render-blocking Tailwind utility chunk wastes 462–635 ms on every route; inlining 4–6 KB of the most-used utilities into a `<style>` block in `layout.tsx` would eliminate one round-trip from the critical path (100–200 ms further LCP geo-mean reduction expected, medium architectural risk — need a strategy to keep inlined CSS in sync with the build); (2) **`/library` DOM weight reduction** via Intersection Observer lazy-render of the 98 primitive showcases (300–500 ms `/library` LCP win, medium risk); (3) **italic `font-display: optional`** — brand call, saves italic from the critical path on every route (italic is used only on /foundations 128 px display + handful of emphasis spans); (4) **real iOS Safari + real Android Chrome** verification — headless Chromium under Lighthouse may not reflect real-device behavior; (5) **R9** — reduced-motion + high-contrast OS-mode contracts (separate axis from R8). **v0.13.3 was the predecessor — R7 pipeline-state + lint-hygiene + pre-commit SSoT regen + mobile-perf baseline.** [ADR 0026](_meta/decisions/0026-sd-pipeline-lint-hygiene-pre-commit-v0133.md) codifies the seventh round of the audit-cycle ladder. R7's tooling axes: `pnpm build` verbose log + `pnpm lint` (now 7 rules including `lint:token-naming`) + a `simple-git-hooks` pre-commit hook for SSoT regen + Lighthouse 12 mobile-profile against a local `pnpm start` production build. **The big four R7 closures:** (1) **SD source-exclusion fix** — `style-dictionary.config.ts` lightConfig.source used `!path` exclusion globs which SD v5's `lib/utils/combineJSON.js:62-65` silently ignores (each source-array entry runs through `globSync` individually, so `!path` matches nothing while the main `**/*.tokens.json` glob still pulls the excluded file). Fix: pre-resolve via `globSync(...)` + `Array.filter(regex)` outside the config object. Same fix applied to `darkConfig.source` for audit-resilience. Plus deleted the duplicate `shadow.accent-glow` self-alias in `semantic/shadow.tokens.json` lines 88–91 (R6 leftover dead code creating a circular reference). **93 collisions + 1 reference error → 0 actionable** (remaining 10 informational warnings are `$description` field-level merges; SD picks one description for shared parent paths and the leaf token values are unaffected). (2) **Token-naming kebab-case migration** — 45 paths across 16 component-token files + 2 semantic files. Component-root renames: `datePicker → date-picker`, `fileDropzone → file-dropzone`, `numberInput → number-input`, `otpInput → otp-input`, `passwordInput → password-input`, `rangeSlider → range-slider`, `tagsInput → tags-input`, `timePicker → time-picker`. Plus 37 sub-path renames (`caretColor` / `caretRotation` / `maxHeight` / `minHeight` / `minWidth` / `iconColor` / `navColor` / `borderWidth` / `borderStyle` / `iconSize` / `browseLink` / `textDecoration` / `fileRow` / `iconLeading` / `iconTrailing` / `valueDisabled` / `valueReadOnly` / `litEdge` / `groupSep` / `letterSpacing` / `groupGap` / `ringFocus` / `inactiveHover` / `translateOn` / `translateOff` / `readOnly` ×2 in dark + light). Plus the `rangeSlider.trackHeight` (24 px slot) → `range-slider.slot.height` semantic rename — it was colliding with `range-slider.track.height` (4 px track-bar) at SD output-name level because both normalize to `range-slider-track-height` after `name/kebab`. 16 `component.json` files updated in lockstep + 1 alias-value fix in `input.tokens.json` + 2 deleted-token cleanups in field/component.json. CSS-variable OUTPUT names are unchanged (SD's `name/kebab` normalizes both forms to the same kebab CSS-var). Plus **`lint:token-naming` wired into the `pnpm lint` umbrella** for the first time (was a standalone script with silent fail since v0.8). (3) **`lint:no-primitives` allowlist convention + 45 showcase block-allowlists** — extended the script to honor `lumen-lint-allow: primitives` / `lumen-lint-allow-block: primitives` directives matching the existing convention in `lint-no-arbitrary-typography` + `lint-no-off-grid-spacing`. Added a multi-line block-comment detector so hex/px literals INSIDE `/* ... */` documentation comments don't false-positive (closes the `dashboard-shell.tsx:37` "28px" false positive that was inside a JSX block comment). 45 showcase files block-allowlisted with a documented top-of-file directive — every component-example file (`design-system/02-components/*/examples/primary.tsx`) and audit-dashboard primitive showcase (`audit-dashboard/src/components/primitives/*.tsx`) is annotated. Production audit-dashboard code (`dashboard-shell.tsx`, `tab-nav.tsx`, `app/*` routes) remains under the linter. Plus 2 `lint:no-arbitrary-typography` allowlists (wordmark tracking overrides on landing logo-cloud + commerce store header) + 3 `lint:no-off-grid-spacing` allowlists (mobile iOS-conventional 10 px row padding, pricing-toggle 2 px thumb top offset, stepper 2 px label↔description tight stack). **`pnpm lint` umbrella across 7 rules → 0 violations.** (4) **Pre-commit hook for SSoT regen** — `simple-git-hooks@^2.13.1` registers `scripts/precommit-regen-indices.mjs`: regen `COMPONENT-INDEX.md` when staged `component.json` changes, regen `TOKEN-INDEX.md` when staged `*.tokens.json` changes. Conservative — skipped if no relevant files staged; heavy validators stay on CI. Escape hatch: `SKIP_SIMPLE_GIT_HOOKS=1 git commit ...`. Documented in [CONTRIBUTING.md](CONTRIBUTING.md). **R7 Lighthouse baseline** at [`.audit-runs/2026-05-18-round-7/LIGHTHOUSE.md`](.audit-runs/2026-05-18-round-7/LIGHTHOUSE.md) — mobile-profile audit across all 9 audit-dashboard routes: Perf 94 geo-mean (`/landing` leads at 97), **CLS 0.000 on every route** (R5's `overflow-x: clip` + R6's primitive a11y left zero layout-shift sources), TBT 12 ms (16× under the 200 ms Good threshold), FCP 1225 ms (Good), SI 1397 ms (Good). LCP is the only Needs-Improvement metric (3030 ms geo-mean; `/foundations` + `/library` sit at 3.3 s — likely Satoshi font-load gating display headings). Carried to R8 candidates: font-display strategy review + Satoshi subsetting + real-WebKit verification. **R7 methodology contribution to the audit-cycle ladder** — each round adds a new tooling axis: R1 visual chrome (claude-in-chrome @ desktop), R2 interaction state (claude-in-chrome @ desktop), R3 contract-comparison (claude-in-chrome @ desktop), R4 meta-contract integrity (grep + release-script audit), R5 small-viewport metrics (chrome-devtools-mcp emulate @ mobile 320 px), R6 LLM-docs SSoT + tooling-script hygiene (validate:tokens + a11y probes), **R7 pipeline state + mobile-perf metrics (pnpm build verbose + lint umbrella + Lighthouse 12 mobile)**. R6's rule was *the audit cycle is itself a contract — each round teaches what the next round's tooling axis should be*. R7 extends: *passing validators (validate, lint, build) is a producer-side green; passing consumer-side metrics (Lighthouse, real-WebKit, reduced-motion-mode) is a separate axis. Both ship together.* The ladder ramps both ends — producer-side R4/R6/R7 (LLM-docs drift → tooling-script hygiene → build pipeline + lint umbrella + pre-commit SSoT), consumer-side R1–R3/R5/R7 (rendered UI → small-viewport metrics → mobile-perf metrics). R8+ candidates extend both: real iOS Safari (R8), font-display strategy + Satoshi subsetting (R8a), reduced-motion + high-contrast OS modes (R9), print + export contracts (R10), native pipeline render verification — Swift/Compose/Flutter (R11), Storybook + a11y-tree per-state probes across all 98 primitives (R12). Audit log at [`.audit-runs/2026-05-18-round-7/ISSUES.md`](.audit-runs/2026-05-18-round-7/ISSUES.md). **v0.13.2 was the predecessor — R6 senior-UX audit pass: LLM-docs SSoT additions + drift cleanup + a11y closeout cascade.** [ADR 0025](_meta/decisions/0025-audit-cycle-ladder-r6-llm-docs-ssot-v0132.md) formalizes the audit-cycle ladder at six rounds and ships R6's closures. R6 pivots from "rendered UI bugs" (R1–R5) to **the contracts authoring agents read against** — documentation drift, primitive-layer a11y cascade, tooling-script hygiene, token-validation closure. **Six new SSoT documents** for LLM discoverability: [`COMPONENT-INDEX.md`](COMPONENT-INDEX.md) (auto-generated catalog of all 98 components — `pnpm component-index` regenerates from `component.json` summaries), [`TOKEN-INDEX.md`](TOKEN-INDEX.md) (auto-generated catalog of ~750 semantic + component-bound tokens — `pnpm token-index` regenerates; primitives intentionally omitted per AGENTS.md hard rule 2), [`audit-dashboard/README.md`](audit-dashboard/README.md) (replaces the stock create-next-app stub with proper onboarding), [`audit-dashboard/ROUTES.md`](audit-dashboard/ROUTES.md) (per-route breakdown of 9 routes — purpose, audience, primitives, audit-cycle cross-reference), [`design-system/00-foundations/data-visualization.md`](design-system/00-foundations/data-visualization.md) (chart-type selection table, axes / legend / color rules with the single-green-series brand contract, accessibility, empty / loading / error / stale states), [`design-system/00-foundations/responsive.md`](design-system/00-foundations/responsive.md) (breakpoint table, the layout-viewport contract codifying ADR 0024 for consumers, sub-768 px authoring rules per-area), [`design-system/00-foundations/state-matrix.md`](design-system/00-foundations/state-matrix.md) (13 canonical states + which primitives must support which subset). **87 `validate:tokens` errors → 0** via 6 new/extended semantic token files (new `semantic/size.tokens.json` for 30 component-bound size aliases — avatar / banner / bottom-nav / calendar / drawer / kanban / list / navbar / phone / popover / sidebar / slider / table / tree; new `semantic/color.invariant.tokens.json` for `color.text.{on-action,on-avatar}` + `color.status.{tone}.border`; extensions to motion / shadow / type / color.dark / color.light for `motion.duration.{shimmer,spin}`, `shadow.{elevation,glow.accent,kbd}`, `type.{tabular.nums,code.{sm,md}}`, `color.chart.1-8`, `color.avatar.bg.1-8`). **`pnpm exec tsc --noEmit` (audit-dashboard) → PASS** — 4 stale `.next/types/validator.ts` errors resolved by `.next` cleanup mid-cycle. **`scripts/release.mjs` widened** to bump root `package.json` `"version"`, README plain-Status block, USING-LUMEN.md install URL, README `## What's new — vX.Y.Z` section heading; plus a **safety guard** that refuses to run if `[next]` is already in `CHANGELOG.md` (closes the 0.13.1 → 0.13.2 over-bump trap session 38 hit). **Primitive-layer a11y cascade** — Switch + Checkbox: when `label` prop is provided, the primitive now generates `{controlId}-label`, assigns it as `id` on the `<Label>`, and wires `aria-labelledby` to it on the Radix `<button role="switch|checkbox">`. R5's R5-006 fix only covered the SwitchRow wrapper-with-sibling-label pattern; R6 caught the in-primitive `label` prop has the **same root cause** — HTML's implicit `<label htmlFor>` doesn't propagate to a button-role element. Foundations Switch+Checkbox showcase rendered the visible label but the buttons stayed nameless. Field children pattern (`<Field label="X"><TextInput /></Field>`): the primitive now uses `Children.toArray` + `cloneElement` to inject `id` / `aria-labelledby` / `aria-describedby` / `aria-invalid` on the first valid React-element child — the inner TextInput rendered its own `<input>` with its own id, so the htmlFor→id link was broken. NumberInput + TagsInput now accept `id` + `aria-*` props and forward to the inner input. RangeSlider ships `aria-label` per thumb (`Range: minimum (val)` / `Range: maximum (val)`). TypeToConfirm wires `<label htmlFor>` + `aria-describedby` to the help-text + `aria-invalid` toggle when the phrase doesn't match. **Post-fix probe across all 8 routes at 320 × 568 px (chrome-devtools-mcp emulate, mobile, touch): 714 interactive elements total, 0 nameless.** R5 closeout was 0 on 7/8 routes; R6 closes /library's 14 → 0. **Naming-collision cleanup** — `nav.tsx` showcase exports `FAB` / `SplitButton` / `CommandPalette` renamed to `FABDemo` / `SplitButtonDemo` / `CommandPaletteDemo` (matches established convention — NavbarDemo, SidebarDemo, FooterDemo). The renames retire the export-name collision with the canonical primitives in `fab.tsx` / `split-button.tsx` / `command-palette.tsx` (an LLM grepping `export function FAB` previously got two hits with no way to distinguish canonical from demo). Single consumer `library/client.tsx` updated. **LLM-docs drift cleanup** — CHANGELOG deduped (removed spurious empty `## [0.13.2]` stub session 38 left in place from `release.mjs patch` on an already-hand-bumped VERSION); README Status line → v0.13.1 narrative + 24 ADRs (was stuck at v0.13.0 / 23 ADRs because the release-script regex caught the bold-chip format but not the plain multi-space `Status:` block); README `## What's new — v0.13.1` section added (was at v0.13.0); USING-LUMEN.md `35 components` → `98 components` across 6 sites (TOC entry, ASCII art at §2, §5 heading + body, §12 totals table, three-sentence summary at the appendix's mental-model paragraph); USING-LUMEN.md install URL `lumen/v0.12.4/registry` → `lumen/v0.13.1/registry`; USING-LUMEN.md §1 status block tagline now reflects R5 + ADR 0024 content (was R4); `llms.txt` + `llms-full.txt` narrative taglines hand-rewritten to lead with R5 + ADR 0024 (the chips were bumped correctly per ADR 0023 lockstep but the narrative taglines drifted); root `package.json` `"version": "0.12.4"` → `"0.13.2"` (was 7 patches stale — release.mjs only bumped VERSION + lib/version.ts pre-R6). **Methodology contribution** — R6 adds **LLM-docs SSoT + tooling-script hygiene** as the sixth axis to the audit-cycle ladder: R1 static @ desktop (chrome bleed → v0.12.7) → R2 interaction @ desktop (Commerce variant pickers → v0.12.8) → R3 contract-comparison @ desktop (Calendar dynamic / iOS StatusBar / Tool preset client island → v0.12.9) → R4 meta-contract integrity @ desktop (LLM-docs version drift → v0.13.0 / ADR 0023) → R5 small-viewport metrics @ mobile (layout-viewport inflation → v0.13.1 / ADR 0024) → **R6 LLM-docs SSoT + tooling-script hygiene → v0.13.2 / ADR 0025**. The methodology rule extends from R5's *a carried blocker is a tooling hypothesis, not a fact* to R6's *the audit cycle is itself a contract — each round teaches what the next round's tooling axis should be*. Audit log at [`.audit-runs/2026-05-18-round-6/ISSUES.md`](.audit-runs/2026-05-18-round-6/ISSUES.md). **v0.13.1 was the predecessor —** R5 responsive safety net + a11y closeout + primitive coverage gap. [ADR 0024](_meta/decisions/0024-responsive-safety-net-v0131.md) closes R4's carried sub-768 px responsive sweep blocker. R5 swapped tooling from `claude-in-chrome` (whose `resize_window` doesn't propagate to `window.innerWidth`) to `chrome-devtools-mcp emulate` (CDP-level viewport — propagates correctly), then walked every route at 320 / 375 px. The discovery: layout-viewport inflation. At 320 px, every Lumen route reported `innerWidth = 509` with `(max-width: 639px)` / `(max-width: 767px)` media queries inactive — every `sm:` and `md:` Tailwind utility broken silently because the matcher reads the inflated viewport, not the device viewport. Root cause: descendants whose intrinsic min-content exceeded the device viewport (foundations typography "Stop re-designing." at 128 px needs ~440 px; library showcase Cards need ~484 px) inflated the layout viewport per CSS Working Group spec. **Fix shipped (ADR 0024):** `html, body { overflow-x: clip }` at `audit-dashboard/src/app/globals.css`. `clip` (not `hidden`) was chosen because it doesn't establish a new scroll container — the v0.12.6 sticky-header chrome continues anchoring to the document scroll root. Verified at 320 px: `innerWidth=320`, `sm:`+`md:` breakpoints fire. Plus per-element defense-in-depth: `TypeRow` grid cell at `minmax(0,1fr) ... min-w-0 overflow-hidden`; `dashboard-shell.tsx` containers swap `max-w-max` → `max-w-screen-2xl` at three sites. **R5 also closed an a11y wave** the desktop walk missed: Switch + Checkbox primitives now accept `aria-label` / `aria-labelledby` (HTML's implicit-label association doesn't propagate to a role-overridden Radix `<button role="switch">`); ProgressRing without `label` ships `aria-hidden` instead of empty `role="img"`; DatePickerCalendar empty cells render as passive `<span aria-hidden>` instead of nameless disabled `<button>`; ProductGallery thumbs ship the tablist + tab pattern; PricingToggle / Kanban "+" / ChatComposer "+" / mobile-inbox search all gained `aria-label`. **R5 also closed the R4-deferred primitive coverage gap:** 5 new `component.md` files (`icon-button`, `button-group`, `split-button`, `command-palette-button`, `fab`) + 7 new `examples/primary.tsx` files (`field`, `textarea`, `select`, `checkbox`, `radio-group`, `switch`, `validation-message`). Plus: new `--label-overlay-strong` / `--label-overlay-fg` semantic tokens (theme-invariant, for labels-on-arbitrary-color UX). Color-literal hygiene: SwatchRamp `rgba(0,0,0,0.55)` retired; ColorPicker legacy `#171A18` (v0.11 obsidian-mint, retired per ADR 0020) replaced with `#0D0D0D`; commerce `ColorSwatchSelector` "Brick" `#e23b3b` (the v0.9-retired red-5 that fails AA at 3.94:1) replaced with `#a8403a` (deeper brick, AA-pass). Audit log preserved at [`.audit-runs/2026-05-18-round-5/ISSUES.md`](.audit-runs/2026-05-18-round-5/ISSUES.md). Methodology contribution: each R-round of the audit-cycle ladder ramps the tooling along with the surface coverage — R1+R2+R3+R4 walked at desktop with `claude-in-chrome`; R5 walked at mobile with `chrome-devtools-mcp`. Future cycles may add real-mobile-Safari for iOS-specific viewport bugs, offline / slow-network for loading-state correctness, reduced-motion + high-contrast for a11y-mode contracts. **v0.13.0 was the predecessor —** LLM-docs version lockstep + R4 comprehensive audit. [ADR 0023](_meta/decisions/0023-llm-docs-version-lockstep-v013.md) extends the v0.12.5 single-source-of-truth contract from the runtime UI to the LLM-discovery layer. `scripts/release.mjs` now rewrites the version chip in `llms.txt`, `llms-full.txt`, `README.md`, `USING-LUMEN.md`, and `PRIMITIVE-COVERAGE.md` in lockstep with `VERSION` and `audit-dashboard/src/lib/version.ts`. The R4 audit caught those five files reading v0.12.5 / v0.12.6 banners while the runtime was already at v0.12.9 — exactly the v0.11.13 palette-footer drift class repeating one architectural layer up. The pattern-matching in `release.mjs` is INTENTIONALLY narrow — each regex includes enough context (`**Status:`, `> **`, `VERSION ←`, `Generated YYYY-MM-DD for Lumen vX`, `Last reviewed against actual repo state`) to disambiguate the current-version banner from historical-version prose, so the 200+ casual `v0.12.9` mentions in CHANGELOG narrative + ADR titles + prose body stay untouched. `AGENTS.md` and `CLAUDE.md` top callouts are EXEMPT from the script — they carry per-release narrative prose (this paragraph) that needs human authorship per cycle; contributors hand-rewrite them as part of the release PR. The other half of v0.13.0 is the **same-day R4 comprehensive live audit** — every route walked top-to-bottom in both dark + light mode at 1501×812 px against the Edge browser on Personal Mac via the Claude in Chrome MCP. R1 (v0.12.7 — chrome bleed), R2 (v0.12.8 — Commerce PDP variant pickers), R3 (v0.12.9 — Calendar dynamic / iOS StatusBar / Tool preset client island) had already shipped the visual + interaction surface fixes; R4 confirmed the system is now in a clean steady state. **R4 documented as by-design (not bugs):** BulkActionBar inverse pattern (`bg-[var(--surface-inverse)]` → bright white in dark mode) is the intentional "selection rail" pattern, analogous to Gmail / GitHub / Linear bulk-action bars in their respective base themes; the token contract "inverse = always opposite of canvas" is functioning correctly. Commerce PDP main image placeholder squares are deliberate showcase pattern. Foundations italic display "Stop re-..." cropping at viewport edge is intentional (display-2xl ceiling at 128 px is *big enough to crop* — that IS the visual statement). Edge browser profile-avatar overlap at bottom-left is browser chrome, not Lumen UI. Carried R3 blocker: sub-768 px responsive sweep still blocked — Claude in Chrome `resize_window` doesn't propagate to `window.innerWidth`. Audit log preserved at [`.audit-runs/2026-05-18-round-4/ISSUES.md`](.audit-runs/2026-05-18-round-4/ISSUES.md). **v0.12.9 was the R3 fix pack** — three real bugs fixed: (1) `DatePickerCalendar` now computes month/year/today/selected dynamically from `new Date()` instead of hardcoding `i-2` cell offset + `isToday=12` (which only landed correctly for whatever month the showcase was authored against — the live audit caught day "1" in the Thursday column even though May 1, 2026 is a Friday). (2) iOS `StatusBar` drops its `carrier = "Verizon"` default — modern iOS 17+ doesn't render carrier in the status bar at all, and the 112×28 px dynamic-island blob was truncating "Verizon" to "...on" in every iOS PhoneFrame. (3) Tool surface preset list extracted to `tool/presets.client.tsx` client island so clicks actually move the active accent off Standard LTL — prior render hardcoded `{name:"Standard LTL", active:true}` with no `useState`, same class of bug as v0.12.8 Commerce variant pickers on a peak surface. Round 3 of the same-day live audit walked picker / sidebar-list / dynamic-render surfaces that R1 (chrome-bleed → v0.12.7) and R2 (Commerce PDP → v0.12.8) skipped. See [CHANGELOG.md](CHANGELOG.md) for full v0.12.9 entry + the deliberately-not-changed list (pricing card hover lift works via Tailwind v4 `@layer`-wrapped `translate:` property; modal-layer "showcases" on /library are visual shape references not triggers; SaaS segmented + /tool switches all toggle correctly). v0.12.8 was the **interactive variant pickers fix pack** — Commerce PDP `Buy` panel extracted to a client island so the color + size pickers wire real `useState` for selection; clicks now move the swatch ring and update the "Color · {name}" label and the size border instead of failing on the first click. Round 2 of the same-day live audit went deeper than round 1 (visual / chrome-bleed) — every clickable, hoverable, expandable primitive across all eight surfaces in both dark and light mode. Most primitives passed; the Commerce variant pickers were the one real interaction defect that surfaced, and it ships fixed in this release. See [CHANGELOG.md](CHANGELOG.md) for the full v0.12.8 entry + the "deliberately not changed" list. v0.12.7 was the **sticky-nav chrome-bleed fix pack** — header swapped to `.lumen-glass-strong` + `saturate(110%)` so primary-CTA lime halos stop tinting the chrome when scrolled close to the header edge; `Radio` primitive falls back to `defaultChecked` when no `onChange` to silence a React "checked-without-onChange" warning on `/library`. Brand anchors unchanged from v0.12.0: Spring Green `#00FA8A` (accent), Obsidian `#0D0D0D` (neutral dark canvas, replaces v0.11's `#171A18` Obsidian Mint), `#E6E6E6` (light anchor / primary text on dark), `#FAFAFA` (paper). v0.12 retunes the brand canvas to neutral near-black ([ADR 0020](_meta/decisions/0020-obsidian-recolor-mint-retired-v012.md)); v0.12.1 — Card corner-clip contract ([ADR 0021](_meta/decisions/0021-card-corner-clip-contract-v0121.md)); v0.12.2 — primary-button hover bloom dialed down ([ADR 0022](_meta/decisions/0022-hover-glow-ladder-retune-v0122.md)); v0.12.3 — Tailwind v4 arbitrary-translate fragility retired (cascade-fix to ADR 0015/0016 — inline `style.left` / `style.transform` for position math); v0.12.4 — three primitive-layer fixes: InlineTabs pill `overflow-hidden`, Combobox dropdown portaled, global `:focus-visible` gains `outline + offset` on top of soft `box-shadow` halo. **v0.12.5** lands five additions caught by a two-round live visual audit (round 1 walks routes statically, round 2 triggers every overlay): (1) new [`audit-dashboard/src/lib/version.ts`](audit-dashboard/src/lib/version.ts) hoists the user-facing version label to a single constant — every consumer (header pill, palette footer, foundations brand-voice samples, library / tool / foundations badges) reads from it; the audit caught the command-palette footer reading `Lumen v0.11.13` three minor versions stale because nobody had grepped the literal — the constant retires that whole class of drift; (2) iconography hover lifts icon glyph to `text-accent` + tile border to `border-accent` (teaches "green appears at action" visually); (3) pricing card peak-end lift on landing (Starter / Enterprise: `shadow-md` + `border-default` + `-translate-y-[1px]`; Operator: soft accent glow); (4) landing FAQ disclosure caret migrates from Unicode `▾` to lucide `ChevronDown`, with the new `.lumen-summary` + `summary.list-none` CSS rule that suppresses the native browser disclosure marker on every browser; (5) privacy scrub — `Sokolovsky` / `Tengariya` retired from 9 sites in 5 files, replaced by synthetic operator names. v0.12.5 ships without a new ADR — five surgical fixes, all consequential follow-ups to ADRs 0007 + 0009 + 0018. Three v0.11 foundations [`hierarchy.md`](design-system/00-foundations/hierarchy.md), [`first-impression.md`](design-system/00-foundations/first-impression.md), [`micro-interactions.md`](design-system/00-foundations/micro-interactions.md) still apply; principles still 7. The single-accent rule (Spring Green only) is unchanged across v0.11 → v0.12.5. See [ADR 0018](_meta/decisions/0018-premium-psychology-recolor.md) (recolor rationale, amended by 0020) and [CHANGELOG](CHANGELOG.md) for the full v0.12.x story (v0.12.3 / v0.12.4 / v0.12.5 ship without new ADRs by design — they're consequential follow-ups, not architectural shifts).

## Skill priority for this repo

When working in this repo, use these skills (when available):

- **`ui-styling`** — for Tailwind / shadcn / Radix work. The Lumen system is built on this stack.
- **`design`** / **`design-system`** — when extending tokens or components.
- **`engineering:code-review`** — before merging any PR that touches a component or token.
- **`engineering:testing-strategy`** — when adding test coverage to a component.
- **`vercel:react-best-practices`** — for any Next.js / React work in `/audit-dashboard/`.

## Token query workflow

When a user asks "what does X look like" about a Lumen component:

1. **Read three files in parallel:**
   - `design-system/02-components/{name}/component.json` (the contract)
   - `design-system/02-components/{name}/component.md` (the prose)
   - `design-system/02-components/{name}/examples/{platform}.{ext}` (the code)
2. Cite token paths with backticks: `color.surface.default`, never copy raw values into prose.
3. If the user is on a specific platform, prioritize that platform's example.

## Naming + voice

- Use Lumen's voice: short, declarative, numerate. (See `design-system/00-foundations/voice-and-tone.md`.)
- When generating button labels, helper text, or any UI copy, run them through the rules in `design-system/04-content/ui-writing-style.md`.
- Avoid the banned phrase list in `design-system/04-content/microcopy.md` § "Banned phrases".

## Generating new components

When asked to create a new component:

1. Check if a similar component already exists (search `design-system/02-components/`).
2. If new, follow the prompt fragment in `_meta/prompts/new-component.md`.
3. Create `./{name}/component.md` and `./{name}/component.json`.
4. JSON must validate against `_schema/component.schema.json`.
5. Add at least one `web-react` example.
6. Add a registry sidecar at `_registry/{name}.json`.
7. Add to `_registry/registry.json` items array.
8. Add a CHANGELOG entry.
9. Surface the work for human review (DRAFT-PR trust level).

## Cross-cutting concerns

- **License.** Satoshi is ITF-FFL — free for commercial use, must self-host, must NOT redistribute the font files in any public repo. Don't commit the font files to a public git repo.
- **Vault context.** This is a Warp internal repo, not the user's Obsidian vault. The vault rules in user's global CLAUDE.md don't apply unless the user explicitly invokes a vault skill.
- **Privacy — synthetic names only in fixtures and demos.** No real-person names in tokens, comments, fixtures, or examples. Use generic operator names. Carrier names are safe (Sterling LTL, Estes Express, ODFL, Saia, FedEx Freight, ABF, Old Dominion, Yellow are all real carriers — public B2B names, not customer identities). The v0.12.5 audit retired `Daniel Sokolovsky` / `Neel Tengariya` / their initials from 9 sites in 5 files (`ai.tsx` CommentThread, `foundations/page.tsx` Avatar + AvatarGroup demos + caption sample, `saas/page.tsx` AvatarGroup, `commerce/page.tsx` review fixture, `library/client.tsx` Avatar + AvatarGroup + Reaction-bar) — replaced with synthetic `Avery Mercer` (`A Mercer`, `Mercer A.`, `AM`) and `Kai Morgan`. When generating new fixtures, use one of these synthetic operator-name patterns or invent another that doesn't map to a real person referenced in the user's `CLAUDE.md` (the vault). Avatar palette is name-hashed, so deterministic colours follow whatever name you ship.
- **v0.11 hierarchy first.** Before generating any new section, page, or component layout: read [`hierarchy.md`](design-system/00-foundations/hierarchy.md) and apply the three-tier rule + the 1.5–2× weight rule. Equal-weight noise is the most common failure mode of LLM-generated UI; principle 2 is the cure.
- **v0.11 hero first.** Any landing or hero surface flows through [`first-impression.md`](design-system/00-foundations/first-impression.md) — three questions answered (what/who/why), three checks passed (branded chrome, single focal point, no layout shift), in 50ms.
- **v0.11 micro-interaction catalog.** Hover, focus, validation, success — read [`micro-interactions.md`](design-system/00-foundations/micro-interactions.md) before designing any state change. Spend motion budget on functional moments; save it from decoration.
- **v0.12.4 floating-UI portal default.** When generating a Combobox / Select / Popover / Dropdown / Tooltip / Calendar primitive — or any new floating panel — portal to `document.body` from day one via `createPortal(<div style={{ position: 'fixed', top, left, width, zIndex }} />, document.body)` with `getBoundingClientRect()` re-tracked on scroll (capture phase) + resize. Never render the panel as an inline `<div absolute>` — Showcase frames, `<Card padding="none">`, glass surfaces, and scroll containers all clip it. See AGENTS.md hard rule 10. Outside-click dismiss must exempt the portaled list.
- **v0.12.4 focus-ring contract.** When generating any new `:focus-visible` rule, include `outline` for structural visibility (`outline: 2px solid var(--lumen-lime-a64); outline-offset: 1px;`) AND `box-shadow` for the soft brand halo. Box-shadow alone fails inside corner-clipped containers (the regression v0.12.4 closed). Outline alone loses the soft alpha-blended brand character. The `.lumen-btn-primary:focus-visible` dual-ring is exempt — it declares `outline: none` and wins via CSS specificity by design (preserves ADR 0016's brand visual). See AGENTS.md hard rule 11.
- **v0.12.3 inline-style for position math.** When generating a toggle / switch / swipe-row / handle / thumb / calendar-nav primitive, write position math as inline `style.left` (or `style.transform`) with a native `transition` declaration. Do NOT use Tailwind arbitrary `translate-x-[Npx]` classes — Tailwind v4's content scanner has been observed to drop them. See AGENTS.md hard rule 12. Use the system's `cubic-bezier(0.2, 0, 0, 1)` decelerate easing.
- **v0.12.4 corner-clip pattern.** When generating a rounded container (Card, TabsList, Pill track, Capsule frame, Sheet) that hosts children with their own backgrounds and smaller-radius corners, compose `overflow-hidden` on the parent. The Card primitive does this on `padding="none"`; the InlineTabs `pill` variant does this on its `TabsList`. The contract is "child clips to parent's rounded shape," not "child matches parent's radius."
- **v0.12.5 version constant — single source of truth.** When rendering a version label in any new TSX (header pill, footer, palette footer, brand-voice mono-cap, badge), import from [`@/lib/version`](audit-dashboard/src/lib/version.ts) — never hardcode the literal. `LUMEN_VERSION` (`"v0.12.5"`) for patch-level chips; `LUMEN_VERSION_MAJOR_MINOR` (`"v0.12"`) for inline references that don't need the patch; `LUMEN_VERSION_MAJOR_MINOR_UPPER` (`"V0.12"`) for explicitly-uppercased brand-voice tokens (`SYSTEM V0.12 · LIVE`). Comments in CSS / TSX about historical versions stay literal; renderable strings don't. See AGENTS.md hard rule 13.
- **v0.12.5 accordion summary marker contract.** When generating any custom accordion (`<details>`/`<summary>` with a lucide chevron icon at the END of the summary), apply `class="lumen-summary"` on the summary element — this triggers the `globals.css` rule that suppresses the native browser disclosure marker on every browser (Chrome / Safari / Firefox via standard `::marker` + `list-style: none`; pre-2022 webkit via `::-webkit-details-marker { display: none }`). Without the class, the native triangle renders BEFORE the summary text and competes with your custom chevron — two arrows, only one of them on the brand stroke ladder. The Tailwind utility `list-none` triggers the same rule pair. See AGENTS.md hard rule 14.
- **v0.12.5 iconography accent-on-hover.** When generating an interactive icon tile (icon library / quick-action tile / single-icon button card), the hover state must lift icon glyph color to `var(--text-accent)` and tile border to `var(--border-accent)` — this teaches the brand rule "green appears precisely at action" visually. The icon stays neutral at rest; the moment of interaction is when accent appears. See foundations §Color "Accent in context" + the canonical implementation in `app/foundations/page.tsx` iconography section.
- **v0.12.5 pricing card peak-end lift.** When generating an interactive card at a peak moment (pricing tier, plan selection, primary action card, decision tile), apply hover lift via the existing system tokens — non-highlighted cards: `shadow-md` + `border-default` + `-translate-y-[1px]`; highlighted cards: layer `var(--shadow-glow-accent)` on top of the rest-state lifted shadow. 140 ms `motion-fast` + standard easing — decelerate-not-bounce per ADR 0016. The pricing decision is a peak moment per Premium Psychology principle 3 — the card MUST respond. Static pricing cards are an LLM-UI smell (the model knows they exist but doesn't know they're peak moments).
- **v0.13.4 Satoshi-subset codepoint contract.** When generating new audit-dashboard content, **assume Latin-1 coverage only** for the bundled Satoshi VF (`audit-dashboard/src/fonts/Satoshi-Variable.woff2` + `Satoshi-VariableItalic.woff2`). Western European text (français, español, português, Deutsch, italiano, Köln, façade, naïve) renders correctly in Satoshi. **Eastern European text (Polish ł, Czech ř, Hungarian ő, Romanian ș, Turkish ğ) falls through to system Arial** — visible character-by-character contrast against surrounding Satoshi. If a future demo needs Eastern European text in Satoshi, add the required codepoints to the keep set in [`scripts/subset-satoshi.mjs`](scripts/subset-satoshi.mjs), re-run the subsetter, and update the backup directory. The keep set covers ASCII + Latin-1 + General Punctuation + Currency + Letterlike + arrows + used Math + geometric shapes + ✓ +  Apple PUA — 230 codepoints, 303 glyphs. **Every Lumen-referenced OpenType feature stays preserved**: ss01–ss04, tnum, lnum, liga, calt, kern, etc. Variable axis: wght 300–900. See ADR 0027 + AGENTS.md.
- **v0.14 R8c lazy-mount on DOM-heavy routes.** When generating a new audit-dashboard route (or refactoring an existing one) that renders > 1500 DOM nodes / > 30 primitive showcases / > 60 cards, wrap the below-the-fold `<Section>` blocks in [`<LazyMount>`](audit-dashboard/src/components/lazy-mount.tsx). First 2 sections stay eager (above-the-fold). Sections 3+ get `<LazyMount placeholderHeight={500}>` with reserved height for zero CLS. The component is SSR-safe (server renders the full DOM for SEO; client lazy-mounts on scroll-into-view). See AGENTS.md hard rule 17.
- **v0.14 R9 + R10 OS-mode + print at the system layer.** Lumen's globals.css ships three system-level media-query blocks: `@media (prefers-contrast: more)`, `@media (forced-colors: active)`, `@media print`. These re-bind shared `:root` tokens for the corresponding OS mode. When generating any new primitive or layout, do NOT hand-author per-component overrides for these modes — the system-level blocks cascade through every primitive automatically. Per-component opt-out only when the system block produces a wrong result (brand-mark SVG that needs forced-color-adjust: none, etc.) and documented in the component.md. See AGENTS.md hard rule 18.
- **v0.14 R8d italic family fallthrough + R11 i18n scaffold.** After R8d, italic Satoshi lives in a separate next/font family (`satoshi-italic`) with `display: "optional"` + `preload: false`. The `--font-sans` chain in globals.css includes both regular + italic families; `font-style: italic` finds the italic-style face via family-chain fallthrough. Do NOT add manual `@font-face` blocks for italic. For i18n / RTL: Lumen has NOT been visually audited at `dir="rtl"` — consumer apps targeting RTL locales should run their own audit. Use logical CSS properties (`margin-inline-*`, `padding-block-*`); apply `data-rtl-flip` to directional icons; apply `data-numeric` to LTR-pinned numeric content inside RTL paragraphs. See AGENTS.md hard rule 19 + `design-system/00-foundations/internationalization.md`.
- **v0.13.5 critical-CSS inlining via Next.js `experimental.inlineCss`.** The audit-dashboard's [`next.config.ts`](audit-dashboard/next.config.ts) sets `experimental.inlineCss: true` (ADR 0028). At build time, Next.js replaces every prerendered page's `<link rel="stylesheet">` with a `<style data-precedence="next">` block carrying the same CSS content. When generating new Next.js App Router pages or layouts for the audit-dashboard, **do NOT hand-roll a parallel critical-CSS scheme** — no `<style dangerouslySetInnerHTML>` blocks duplicating utilities, no `<link rel="preload" as="style" onload>` race patterns, no manual extraction via penthouse / critters / beasties. The build-time inlining handles every prerendered page universally. Both `@font-face` blocks (handcoded `Satoshi-Fallback` from globals.css + next/font's `satoshi Fallback`) are preserved verbatim in the inlined CSS; the metric-aligned CLS contract (ADR 0010) holds because `size-adjust` + `ascent-override` + `descent-override` are derived from the woff2's hhea + OS/2 tables — font-table-derived, not glyph-derived. **For Lumen consumers building their own Next.js apps**, read [ADR 0028](_meta/decisions/0028-inline-css-mobile-perf-v0135.md)'s trade-off matrix before enabling — the flag is right for single-visit + atomic-CSS regimes (audit-dashboard, marketing landing pages, status pages); it's the wrong call for multi-page apps with high returning-visitor traffic where the cross-page CSS cache benefit outweighs the round-trip elimination. Document the choice in your app's CLAUDE.md. See AGENTS.md hard rule 16.

## Working with the audit dashboard

The audit dashboard at `/audit-dashboard/` is the live reference implementation. It uses every token, every primitive, every component pattern.

- Run with `cd audit-dashboard && pnpm dev`.
- DO NOT run the dev server during heavy file-editing work — Turbopack + many open files can OOM the kernel. Run dev server when actively viewing; kill it before doing batch edits.
- The dashboard's [globals.css](audit-dashboard/src/app/globals.css) is now the de-facto source of truth for built CSS — it carries all v0.4 token mappings, the v0.5 typography utility classes, and the v0.6 `.lumen-field` shell system (~1900 lines). When Style Dictionary's `_build/tailwind/theme.css` is wired (ADR-0001 follow-up), the goal is to derive `globals.css`'s `:root` token block from it; the v0.5+ utility classes and v0.6 shells continue to live in `globals.css` as authored CSS.
- Treat `globals.css` as edit-with-care, not a placeholder.

## When asked to ship a brand voice

The user occasionally invokes `/!boil-the-ocean` or `/!claude-ai-research`. These are personal user skills:
- `/!boil-the-ocean` = do the comprehensive thing, no shortcuts, ship the whole task.
- `/!claude-ai-research` = research deeply, source-grounded, write to `research/` or `notes/` as Markdown.

Honor them.
